diff --git a/app/SiloUI/src-tauri/guest/agent-tools.py b/app/SiloUI/src-tauri/guest/agent-tools.py
index a42d9ae..a605300 100644
--- a/app/SiloUI/src-tauri/guest/agent-tools.py
+++ b/app/SiloUI/src-tauri/guest/agent-tools.py
@@ -328,8 +328,9 @@ def ensure(release, state, *, running=desktop_running, fetch=download, install=b
                      selected_release=result.get('selected_release'))
         if type(value['last_ready']) is bool:
             value['checked_at'] = int(time.time())
-        value['browser_configured'] = (candidate is not None) if result.get('ok') is True else None
-        value['state'] = 'ready' if result.get('ok') is True and value['installation_completed'] is True and value['configuration_generated'] and value['last_ready'] is True else 'attention' if value['installation_completed'] is not None else 'unconfirmed'
+        completed = result.get('ok') is True and value['installation_completed'] is True and value['configuration_generated'] and value['last_ready'] is True
+        value['browser_configured'] = (candidate is not None) if completed else None
+        value['state'] = 'ready' if completed else 'attention' if value['installation_completed'] is not None else 'unconfirmed'
         if value['state'] != 'ready':
             value['reason'] = 'bootstrap_unconfirmed' if value['installation_completed'] is None else 'bootstrap_failed'
     except Exception as exc:
