# Fresh-agent rich range editing

One fresh Codex CLI attempt completed a held-out cooperating ProseMirror task in **57.989 seconds**. This is a single local usability observation, not a reliability estimate or a claim of generic rich-editor compatibility. No prompt tuning, retry selection, model substitution or feedback was supplied during the attempt.

The initial model contained bold `Pré 👩🏽‍💻: `, plain `ancien é`, and italic ` / FIN`. The task asked the agent to replace only the unique middle text with the exact JSON string `"nouveau 日本語\n\tÉté 👩🏽‍💻\n"`, preserve both formatted ends, save through the visible button, verify the result and mention any clipboard side effect. It supplied no offsets, tool sequence, transport option or menu coordinates.

The agent read the installed skill, opened the provided local URL in a temporary owned browser, read the public rich-field model and selected code-point range **10..19**. It chose `transport="clipboard"` and `line_breaks="paragraph"` itself. The application's visible Save handler independently read the actual model, rendered paragraphs and marks; the saved oracle confirmed exact text including tabs/LF/ZWJ/combining context, three paragraphs, unchanged strong prefix and em suffix, and a trusted paste event. Its final response disclosed that CLIPBOARD contained the final nonempty segment, prior contents were not restored and PRIMARY was unchanged. That statement describes the active temporary session; its owner is removed when that session closes.

One recoverable error is retained: the first `desktop_select` ran before focus and returned `FOCUS_CHANGED`, effect none. The agent focused the field, read it again and repeated the intended selection. There was one content-input call, no native-input failure followed by automatic clipboard fallback, and no hidden/source/direct-file access. All command execution was the allowed skill read; all other tool calls were public Luda desktop tools. Three filtered inspections returned 2/5/5 native nodes with truncation honestly reported; the browser text-field provider supplied the exact model. No screenshots were requested.

## Cost and reproducibility

Immutable original evidence: `artifacts/agent-rich/run-1789892996220949502/{result.json,events.jsonl,stderr.log,desktop.log}` in the qualification worktree. The exact compressed CLI trace and compact result/oracle are also committed under `tests/evidence/agent-rich-first-attempt/`. Source fingerprints before/after matched. The authenticated CLI ran as its existing root account without reading or modifying credential files; MCP, Chromium, Xvfb, XFWM, application server and D-Bus ran as ordinary UID 1001 on a private display with fresh XDG directories. The independent save oracle was outside the agent workspace. The only installed workspace material was the current skill. The command used `--ignore-user-config --ephemeral --sandbox read-only`, task-local MCP approval configuration and a 180-second limit.

Versions: Codex CLI 0.155.1, Chrome for Testing 153.0.8010.12, current locked Luda/Playwright dependencies and the pinned offline ProseMirror bundle. The CLI did not expose the resolved model; it remains **unknown**, not inferred from authentication or service. Reported usage was 300,443 input tokens including 265,088 cached (35,355 uncached), and 862 output tokens. These are the CLI's cumulative usage counters, not an estimate from text volume.

There were **13 completed tool calls**: doctor 1, open browser 1, inspect 3, read text 3, select 2, focus 1, type 1 and invoke 1. Public `elapsed_ms` totals **2.990 seconds**; it excludes model reasoning, MCP transport overhead and other wall time. Tool responses contained 17,645 UTF-8 text bytes, of which inspections contributed 8,484, with zero images. The two extra calls around the failed pre-focus selection are concrete recovery friction: discovery did not make `desktop_select`’s focus precondition obvious enough for this agent; this run does not isolate the cause of overall token use or establish an optimal sequence.

## Reuse

Run `scripts/agent_rich_eval.py --executable /absolute/provisioned/chrome --timeout 180` with the existing authenticated CLI account. It creates an ordinary-user private desktop; it makes no authentication or global configuration changes. Three focused oracle tests verify the exact expected model, offset unit and rejection of lost formatting or inconsistent rendered paragraphs. The fixture is test-only, serves pinned offline assets, and exposes no driver-accessible setter. Its save button records actual application state.

After the attempt, the copied fixture package's build script was corrected to reference `app.js`/`app.bundle.js`; application and bundle bytes used in the evaluation did not change. All original traces, the focus error, returned usage and source fingerprints remain retained. No runtime change was made from this observation. An explicit focus-before-selection reminder may help future skill users, but should be evaluated separately rather than retroactively improving this attempt.
