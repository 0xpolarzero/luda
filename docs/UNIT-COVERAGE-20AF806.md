# Unit mapping coverage: 862-test snapshot

Source `20af806` ran 862 tests: 861 passed in the root browser-free environment (one unavailable-build-dependency skip), and 853 passed as the ordinary desktop account (the same build skip plus eight unavailable Codex CLI cases). Both retain unchanged fingerprint `6bf3bec827802a4b50a298142b40d7c0c2af6c079342b5d28090c82639f0d8dd`. The actual wheel regression passed separately with installed locked build tools. Records are `artifacts/qualification/launch-root.json` and `artifacts/qualification/backend-host/launch-ordinary.json`; the [797-test snapshot](UNIT-COVERAGE-9E17932.md) retains earlier failures and corrections.

All 17 private native suites passed at this source, including bounded launch observation, public MCP, cancellation, resource checks, key/pointer guards, target replacement and session state (`run-1789896961778015352`). All five private browser suites also passed together: HTML/lifecycle 17.804 seconds, native rich text 24.084 seconds, clipboard rich text 63.969 seconds (51 assertions), actual location-permission denial 3.719 seconds, and partial rich progress 5.444 seconds (`run-1789896961742598893`). Both runners retained the same unchanged fingerprint.

Actual managed installation produced release `0.1.0-fc49ebcfe821d846`. All 48 runtime modules match source bytes and the exact module set. Under deliberately polluted caller `PYTHONPATH`, installed launcher/MCP editing and EOF cleanup passed; all four stage cleanup receipts confirm cleanup. The installed LICENSE matches the repository exactly. Evidence is retained in `artifacts/managed-launch-20af806/`, with the same unchanged fingerprint. Earlier packaging at `4d4a6d2` separately verified exact runtime modules, LICENSE in wheel and sdist, all 51 selected retained evidence assets, and absence of node_modules. The original missing-license archives remain in `artifacts/package-00c6aae/`.

Hosted source `00c6aae` passed desktop contracts, native applications, optional media, Codex registration and Silo registration. Its browser matrix passed four suites but failed clipboard recovery at 198 seconds with BUSY after browser exit. This established that the previous 180-second watchdog was too short for the measured hosted workload and exposed a separate fixture assumption: process exit does not prove server finalization. The corrected fixture waits for the cancelled operation's new final history receipt and recovering:false, then opens once. Original failures remain preserved. Hosted source `20af806` subsequently passed all five triggered workflows: desktop, native applications, optional media, Codex registration and [owned browser](https://github.com/0xpolarzero/luda/actions/runs/35502766709). The browser matrix passed all five suites, including clipboard recovery in 202.128 seconds, and its installed release `0.1.0-e93b39d5e87e35b0` matched all 48 runtime modules with all four cleanup receipts true. Downloaded evidence is retained in `artifacts/hosted-browser-20af806-success/` (matrix `run-1789897058417553154`). Silo paths did not change, so its workflow was not triggered at this commit; its latest passing run remains `00c6aae`.

Catalog priorities and qualification remain unchanged: 346 release-unqualified cases, 147 requirements with mapped local assertions, and 199 without mapped unit evidence. New mappings cover narrowly verified rich segments and observed geometry; dispatch counters do not prove application outcomes, and observed restore geometry does not implement automatic prior-geometry tracking. First-attempt delayed-save, terminal-confirmation and stale-screenshot evaluations have separate oracle and visual-review records. Generic native IME guarding, broader provider capabilities, fresh Mac/microsandbox and shipped Silo acceptance remain outstanding.
