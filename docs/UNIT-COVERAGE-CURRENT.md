# Current integrated verification

Source `cdf2fc7` ran 891 tests: 890 passed in the root browser-free environment (one unavailable-build-dependency skip), and 882 passed as the ordinary desktop account (the same build skip plus eight unavailable Codex CLI cases). Both retain unchanged fingerprint `d1a1cd2b624cce2074ac659448902938c88d3f065fb801e89d5b6737964620a1`. Records are `artifacts/qualification/password-range-root.json` and `artifacts/qualification/backend-host/password-range-ordinary.json`. Four subsequent first-attempt password evaluator tests passed separately. The [862-test snapshot](UNIT-COVERAGE-20AF806.md) preserves earlier integrated and hosted results.

Source `b13e5a1` subsequently ran 904 root browser-free tests: 903 passed, with the same build-dependency skip (38.277 seconds). All 17 private native suites also passed, and the focused matrix passed geometry, restore comparison, ranges and recycled rows in 2.950, 5.544, 12.907 and 8.869 seconds respectively. These runs share unchanged fingerprint `0adbb861b32f56a27c4a453458e702ebb263244f9ba2caf4a9c4f054c4533ef0`; records are `artifacts/qualification/restore-root.json`, headless `run-1789899187372758954` and matrix `run-1789899187339033329`. This later source includes restore-history comparison; its root result does not substitute for a new ordinary-account full-unit run.

At source `3218a48`, all 17 private native suites passed (`run-1789898853837974254`). The broader matrix passed HTML/lifecycle in 17.359 seconds, native rich text in 24.042, clipboard rich text in 63.036, permission denial in 3.846, partial rich progress in 5.627, and range selection in 13.084. Its password fixture stopped before browser startup because the new artifact directory was not writable by the desktop account; that failed run remains `run-1789898853808705587`. After creating that directory for the test account, the password suite passed in 20.726 seconds (`run-1789898958141704345`). All three runs retained unchanged source fingerprint `61b059863a7f51a058aaab1401d1494a5a3cae455f577c529aa6c076f60344bf`; the failed matrix is not relabeled as an all-green run.

Actual managed installation at that source produced `0.1.0-664c3514bb3f31ee`: all 49 runtime modules match source bytes and the exact module set, installed launcher/MCP editing and EOF cleanup pass, and all four stage cleanup receipts are true. Evidence is retained in `artifacts/managed-password-range-3218a48/`. The three actual isolated wheel-build regressions also passed with locked build tools at `76f9085`.

Hosted source `76f9085` passed desktop contracts, native applications, optional media, Codex registration and Silo registration. Its [browser workflow](https://github.com/0xpolarzero/luda/actions/runs/35503837414) failed during private-directory cleanup after the clipboard fixture's 51 assertions passed: a disconnected document-portal FUSE mount caused ENOTCONN. A [narrow cleanup correction](../tests/evidence/hosted-portal-cleanup/README.md) now detaches only the exact owned private portal mount and verifies removal. Its real disconnected-FUSE reproduction and focused contracts pass; hosted source `53d8098` is under verification. Passing UI assertions alone do not make cleanup successful.

Hosted `53d8098` desktop verification has passed: 902 of 912 unit cases passed with ten environment-dependent skips, all 17 private native suites passed, and the separate matrices passed geometry/restore comparison plus range/recycled rows. The downloaded records in `artifacts/hosted-desktop-53d8098-success/` retain unchanged source hashes. This adds actual AMD64 desktop evidence for the new native behavior; the browser workflow remains a separate pending result.

The [first-attempt password evaluation](AGENT-OWNED-SECRET-EVALUATION.md) used five public tool calls without errors, ordinary focus attempts or submission. Exact content is established by the independent application hash; the retained screenshot separately shows masking and a not-submitted receipt. The original automated visual-review-required result remains unchanged. A separate [first-attempt native range evaluation](AGENT-NATIVE-RANGE-EVALUATION.md) selected the requested range while retaining the prior record, with exact semantic readback and independent selected-record IDs. Range selection, restore-history comparison and recycled-row behavior have separate narrowly scoped reports; their later commits are not retroactively covered by an earlier source snapshot.

Catalog priorities and qualification remain unchanged: 346 release-unqualified cases, 147 requirements with mapped local assertions, and 199 without mapped unit evidence. Generic native IME guarding, broader provider capabilities, fresh Mac/microsandbox and shipped Silo acceptance remain outstanding.
