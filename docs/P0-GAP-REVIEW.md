# Remaining P0 work and scoped evidence follow-ups

The original read-only audit examined `7783721` on 2026-09-20. This follow-up checks source `00c6aae` and the linked evidence. Acceptance text and priorities remain those in [requirements.json](requirements.json); all 346 cases remain release-unqualified. A passing fixture, a unit association and a production delivery claim are different things. [Current source-bound records](UNIT-COVERAGE-CURRENT.md) and the [live inventory](LIVE-COVERAGE.md) retain their own versions and scopes; this document does not promote old results to a new source revision.

## Behavioral/provider limits still open

| Case and acceptance | Current boundary | Useful remaining work |
| --- | --- | --- |
| **EDIT-10:** active IME must be explicitly completed/cancelled or conflicting input refused | Generic native input cannot establish current preedit. [Real GTK probes](IME-COMPOSITION.md) observed Entry focus/type clearing preedit and TextView exact readback while preedit survived and later committed. Native read/type now reports `composition.known:false` and `active:null`; that is honest metadata, **not a guard**. The optional owned-browser guard covers its own explicit scope. | The [GTK3/GTK4 feasibility audit](NATIVE-IME-FEASIBILITY.md) found no authoritative external state channel. An application-cooperating current-state adapter must bind the exact field and preserve active preedit before broader support can be claimed. Do not infer inactivity from engine presence, a successful paste or matching committed text. |
| **ERR-05:** identify completed portion and remaining uncertainty after a partial compound action | [Key progress](KEYBOARD.md) and [rich progress](RICH-PROGRESS.md) now provide bounded final-receipt counts. Native keys count acknowledged dispatches; cooperating rich segments count historical exact readback, with current work uncertain and remaining segments not started. Neither verifies save/commit or manufactures counts after a lost receipt. | Other compound operations remain outside these two implementations. Preserve unit distinctions and uncertainty; inspect current state before any explicit next action. Do not replay a reported remainder automatically. |
| **EDIT-03 / TXT-09**, with **APPS-04:** exact selected-range or empty replacement in Electron | The original Electron non-BMP semantic `desktop_type` clear remains `SELECTION_UNVERIFIABLE`, with lossy Chromium 152 AX selection. A separately explicit public focus→Ctrl+A→Backspace workflow now clears the full field, verified by independent DOM and accessible readback while preserving a sibling entry. [Retained suite](ELECTRON-QUALIFICATION.md) is 18/19, exit 1: the semantic failure stays visible. | The full-field GUI route supplies an application-specific TXT-09 workflow; it does not fix arbitrary EDIT-03 selected-range replacement, semantic offset loss or generic Electron support. Never invert lossy offsets or silently retry an uncertain request. |

## Completed local follow-ups, with remaining scope

- **ERR-05 rich receipts:** actual native and explicit-clipboard cases verify the first segment, reject a later paragraph action, and report 1 verified/1 uncertain/1 not started. Error/history/report metadata excludes payload text. Unit tests cover receipt loss and per-request reset. This closes the original missing-rich-receipt work item, not every partial compound action.
- **INTEL-04 delayed save:** [one fresh-agent attempt](AGENT-DELAYED-SAVE-EVALUATION.md) dispatched Save once, encountered explicit `STALE_TARGET`/`ACCESSIBILITY_UNAVAILABLE` observations during a 20-second storage stall, then verified receipt 1 visually. Independent accepted/saved files contain exactly one exact report. No literal `TIMEOUT` code occurred. The original automated grading failure is preserved alongside separate hashed screenshot review; no retry or prompt tuning occurred.
- **INTEL-06 paste confirmation:** [one fresh-agent attempt](AGENT-TERMINAL-CONFIRMATION-EVALUATION.md) encountered the real xfce4-terminal warning while its passive PTY held zero bytes, accepted the existing dialog and verified the receipt without repeating paste. Exact 64-byte UTF-8/terminal LF→CR input is independently recorded. The original result remains visual-review-required/failed; separate image review supplies the scoped conclusion. It never executes input as shell commands and proves no other terminal or agent success rate.
- **WEB-07:** [actual geolocation permission refusal](BROWSER-PERMISSION-QUALIFICATION.md) uses public browser chrome and an independent permission-state/callback oracle. It does not cover every permission or silently grant unrelated access.
- **SHIP-05:** [installed offline startup](OFFLINE-STARTUP-QUALIFICATION.md) tests pinned installed native MCP in a private network namespace. This is tool startup, not offline cloud-agent operation or every optional backend/network condition.
- **WM-02 / WM-03:** [observed geometry](WINDOW-GEOMETRY-QUALIFICATION.md) reports actual constrained bounds and state and independently observes one GTK3/XFWM restore. Advertised increments were not universally enforced in the fixture; that failure is retained. A later bounded same-generation cache now compares restore with pre-maximize bounds and invalidates on observed changes or other explicit state actions; see the updated geometry report. It never forces geometry and cannot detect external changes that revert between observations. The unit map stays partial.

## Delivery and external acceptance still required

| Cases | What remains | Required deliverable/environment |
| --- | --- | --- |
| **SHIP-01 / SHIP-03**, also **LIFE-01 / SEC-10** | [Silo integration](../integrations/silo/README.md) remains a patch deliverable with its canonical manifest disabled. [Source preparation](RELEASE-ARTIFACT.md) creates an exact committed archive/checksum/provenance candidate locally. A [canonical full-commit GitHub source](../integrations/silo/releases/e08bc91.json) is now available and independently matched to its exact Git tree and guest extractor; URL absence is no longer the blocker. Neither proof configures or installs through Silo GUI, or makes a fresh Codex task discover it. Artifact-preparation tests therefore are **not mapped to SHIP-01/03 acceptance**. | Explicitly select the verified source candidate (or publish separately built reviewed bytes), configure trusted architecture-appropriate browser inputs and integrate the patch into the actual Silo product. Do not enable placeholder URLs or describe a local executable hash as independent supply-chain verification. |
| **ENV-01 / ENV-02 / MCP-01 / SHIP-02 / SHIP-04 / SHIP-07 / EVAL-03** | Hosted AMD64/ARM64, SSH transports, Codex registry tests, Rust/frontend builds and installed wheels are meaningful but do not prove a fresh microsandbox from the shipping Mac app loaded the intended skill. | On an owned Mac/Silo environment, create supported-architecture guests through the GUI, attach a fresh Codex task, verify tool/skill identity, make an independently observed edit, reconnect the same viewer/session and upgrade while preserving documents. Retain first attempts. |
| **LIFE-07 / LIFE-08** and display-specific **GEO-06 / GEO-08 / GEO-10** | Clock injection and process/X-server loss are not VM suspend/shutdown. Logical monitors, controller movement and panning have local evidence; [backend probes](RANDR-BACKEND-REVIEW.md) could not apply certain transforms on tested drivers. | Test actual owned VM host lifecycle and a capable physical/display backend if claiming those modes. GEO-06/GEO-08 explicitly allow unsupported results; implementing every physical transform is not an extra requirement. |

Native IME remains a concrete unresolved behavioral contract. Silo artifact delivery can proceed independently of fresh Mac acceptance; neither a longer identical Xvfb run nor another plain Unicode test resolves those gaps.

## Avoid stale or inflated claims

- Arbitrary selected-range clipboard editing is implemented for **cooperating basic ProseMirror**, not generic rich schemas, frames, protected fields or unrelated Electron apps.
- Same-root-size topology changes are detected; the older [geometry report](GEOMETRY-QUALIFICATION.md) predates [DISPLAY-TOPOLOGY.md](DISPLAY-TOPOLOGY.md).
- AUTH is P1. A missing system-authentication fixture is not a new P0 priority or proof that polkit inherently needs macOS/hardware.
- A criterion allowing explicit unsupported behavior is not missing solely because refusal is the correct result; refusal also must not be described as successful text delivery.
- Priorities and qualification statuses are unchanged. Source-bound first-attempt evidence does not establish a general agent reliability rate.
