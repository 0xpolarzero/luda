# Unit mapping coverage: 755-test snapshot

Source `5cec289` ran 755 unit tests: 754 passed as root and 746 as the ordinary desktop account. Both skipped the actual wheel regression because the development environment lacks build dependencies; the ordinary account also skipped eight unavailable Codex CLI tests. The wheel regression passed separately using the installed release's locked build tools. Both full runs retained unchanged fingerprint `5721f53336276fc958cfe710da0958e0b2384c2d432efb23f5bec2f93fb232b3`.

Records are `root-python-isolated.json` and `ordinary-python-isolated.json` under `artifacts/qualification/backend-host/`. Catalog and mapping remain 346 release-unqualified cases, 145 requirements with mapped local assertions, and 201 without mapped unit evidence. The [745-test snapshot](UNIT-COVERAGE-60DDC75.md) retains browser boundary and earlier qualification details.

The actual managed installation at the same source produced release `0.1.0-c4774b789f0d44a6`. All 46 installed runtime modules matched source, including exact module-set equality. With deliberately polluted caller `PYTHONPATH`, the isolated installer installed the actual package; the isolated provenance check imported it from the selected release. Three build regression tests, including an actual contaminated-wheel comparison, passed under locked setuptools/wheel. A private ordinary-account XFCE session then exercised the installed launcher and MCP, verified Unicode/LF browser edits independently, and confirmed EOF cleanup. Source stayed unchanged throughout. Evidence is retained at `/workspace/luda-managed-ci-13fe785/python-isolated-evidence/`.

Earlier failures are retained alongside it: stale checkout build content contaminated a wheel; caller Python paths caused pip to skip actual installation; and the first CI harness had a missing fingerprint argument. Corrections build from isolated source inputs and isolate installer Python/pip execution. This positive fixture does not exercise replacement after startup or prove descendant cleanup on every outer watchdog timeout.

Silo patch 0018 adds explicit optional browser selection; patch 0019 subsequently fixes an incomplete setup's misleading completed-browser status. Twenty-seven guest integration tests pass after that fix. Hosted native coverage now includes the exact browser-action test alongside ten registration tests and 34 frontend tests. Hosted execution at `460cadc` is pending; no Mac, fresh VM or shipped Silo claim is made.

Integrated browser source `0d089bb` also passed ordinary HTML/lifecycle in 16.472 seconds and cooperating rich editing in 23.053 seconds, with unchanged source in `artifacts/qualification-matrix/run-1789891569778630864/`. The explicit clipboard production route is still undergoing independent review and is not part of this snapshot.
