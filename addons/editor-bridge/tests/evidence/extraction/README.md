# Separate editor bridge extraction evidence

The add-on is installed explicitly and invokes `luda-editor-bridge`, with the real `editor_*` MCP tools. Core Luda does not expose these tools or load the adapter. Both runs used an ordinary Linux account, private Xvfb/XFWM/D-Bus sessions, sandboxed Chromium 153.0.8010.12 and Playwright 1.63.0. No shared desktop was driven.

- Initial source `5912382`: all four suites passed. The inherited harness fingerprint covered core files only; the dedicated worktree remained clean and frozen for the complete run. This is retained as initial evidence, not presented as an add-on-bound hash.
- Final source `06fb161`: all four suites passed, with before/after source identities covering **both core and add-on runtime, application adapter, skill and fixtures**. Source identity was unchanged throughout. Native paragraphs: 22.970 s; clipboard ranges: 60.273 s; hard breaks: 31.087 s; partial progress: 5.420 s. The compressed complete matrix contains exact versions, source file hashes, cleanup outcomes and suite metadata; separate case receipts contain independent application oracles.
- Follow-up `d339c96` changes readiness diagnostics only: unsupported multi-page/frame scope is distinct from missing application registration. All **64** add-on unit tests passed after this change, including focus-default preservation, no add-on import/discovery from core even when installed, separate schemas, owned-window refusal, native-toolbar typing refusal, browser prerequisite readiness, scope diagnosis and the migrated rich editing contracts. The GUI run precedes that diagnostic-only change.

Core's focused ordinary HTML and password regressions passed (22 cases). Full core testing and installed wheel/plugin verification are recorded in the repository's main validation evidence. Skill and plugin manifests passed the skill/plugin validators. These results verify this declared Linux/browser/editor scope; they do not claim arbitrary ProseMirror schemas, frames, browser attachment, native IME detection, saving, or all environments.
