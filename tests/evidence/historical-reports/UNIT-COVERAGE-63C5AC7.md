# Unit mapping coverage: 775-test snapshot

Source `63c5ac7` ran 775 tests: 774 passed as root and 766 as the ordinary desktop account. Both skipped the build regression because the development environment lacks build dependencies; the ordinary account also skipped eight unavailable Codex CLI tests. The actual wheel regression passed separately with the installed release's locked build tools. Both full runs retained unchanged fingerprint `cf55ab7babe517ee7d5ff97b68243e1933def379b77b37793c6d7b4eb0c9e3e1`.

Records are `root-rich-clipboard.json` and `ordinary-rich-clipboard.json` under `artifacts/qualification/backend-host/`. Catalog and mapping remain 346 release-unqualified cases, 145 requirements with mapped local assertions, and 201 without mapped unit evidence. The [755-test snapshot](UNIT-COVERAGE-5CEC289.md) retains isolated-installer and Silo validation details.

At the same source, private ordinary-account MCP suites passed HTML/lifecycle in 16.692 seconds and native rich editing in 24.462 seconds (`run-1789892732076648793`). The new clipboard suite initially failed before launch because its artifact directory was not writable; that failure is retained. With an owned output directory it passed all 46 assertions in 64.090 seconds (`run-1789892797016210241`), with source unchanged. Cases include exact arbitrary code-point selections, surrounding marks, paragraphs, trusted composition refusal, held-input refusal, focus theft, cancellation and recovery.

The superseded physical-key clipboard design had a reproduced interruption defect that left Control held. It was replaced before publication by virtual browser key dispatch. Independent actual interruption immediately after virtual keydown found no held X11 keys and confirmed browser cleanup. Both original failure and corrected evidence remain in `tests/evidence/browser-clipboard-guardian/`. Passing those checks does not make clipboard transfer or focus atomic.

The actual installed workflow produced release `0.1.0-1fd079f0094942e5` with all 47 runtime modules byte-equal to source and exact module-set equality. It passed under deliberately polluted caller `PYTHONPATH`; all four owned CI stages returned successful cleanup receipts. The installed launcher/MCP performed independently verified Unicode/LF browser edits and EOF cleanup. Evidence is retained in `artifacts/managed-rich-63c5ac7/`, with the same unchanged source fingerprint.

All twenty Silo patches apply to a fresh pinned checkout, with canonical guest helper and embedded skill byte-equal to Luda's copies. Twenty-seven guest integration tests pass. Hosted Silo source `460cadc` passed 34 frontend tests, TypeScript, ten registration tests including five actual Codex CLI cases, and the exact native browser-action test ([run](https://github.com/0xpolarzero/luda/actions/runs/35499034314)).

Hosted source `a44b652` passed native apps, media and Codex registration, but failed desktop units and browser IME preconditions. The unit fixture incorrectly assumed the optional Playwright dependency; its correction passed a real browser-free 758-test run (757 passes, one build-dependency skip). The IME environment failure is reproduced and its fixture correction is under validation. Neither failure is silently counted as a hosted pass. Fresh Mac, fresh microsandbox and shipped Silo acceptance remain outstanding.
