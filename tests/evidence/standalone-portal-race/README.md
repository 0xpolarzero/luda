# Private test portal detach race

[Hosted standalone browser run 35508590754](https://github.com/0xpolarzero/luda/actions/runs/35508590754), source830a3c4, passed all eight browser GUI suites. The installed-browser probe also passed Unicode readback and owned-browser process cleanup, but the wrapper failed private document-portal cleanup. Original failed job and separate fixture/cleanup receipts are retained. The old helper did not record the unmount exit or remaining mount table, so the original cause is not established.

A concrete false-failure path was reproduced: a portal can detach after the identity check, causing fusermount to return nonzero although the mount is absent. The helper now observes the mount table after the call regardless of exit code and removes the private directory only if no mounts remain. It reports a nonzero exit separately on successful removal. Remaining/replaced/foreign mounts still fail closed; no repeated unmount or broader target selection was added. Failure receipts now retain the cleanup reason.

Eleven cleanup/wrapper contracts passed. `tests/live_private_portal_cleanup.py --detach-race` created a real disconnected fuse.portal in private user/mount namespaces, performed a real detach immediately before the helper's real unmount attempt, and verified mount and directory absence plus an untouched unrelated marker. Only subprocess ordering was instrumented; mount operations and observations were real. This qualifies the reproduced race, not a retrospective diagnosis of the hosted failure. There is no runtime GUI change.
